//Fetch é uma API que faz requisições a aplicações externas para retornar resultados que desejamos. Quando ele faz a requisição a algo externo, ele retorna um type "cors" que é usado para importar algo

fetch("https://google.com")
.then((result) =>{

})